trigger:
  batch: true
  branches:
    include:
    - v3
    - v3-release
pr:
- v3

jobs:

# Test and deploy on windows
- job: WindowsBuild
  pool:
    vmImage: 'windows-latest'
  steps:

  - checkout: none
  - task: GitHubComment@0
    inputs:
      gitHubConnection: Engineering
      comment: Hi **GitHub**
