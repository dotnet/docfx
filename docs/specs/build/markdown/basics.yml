---
# Hello World
inputs:
  docfx.yml: |
    content:
      include:
        - "**/*.*"
  hello.md: Hello `docfx`!
outputs:
  hello.json: |
    { "content": "Hello `docfx`!" }
---
# Basic markdown syntax
inputs:
  docfx.yml: |
    content:
      include:
        - "**/*.*"
  inline.md: _this_ *is* an ***inline*** `markdown` **test**
outputs:
  inline.json: |
    { "content": "_this_ *is* an ***inline*** `markdown` **test**" }
