{{!Licensed to the .NET Foundation under one or more agreements. The .NET Foundation licenses this file to you under the MIT license.}}

<ul class="nav level{{level}} navbar-nav">
  {{#items}}
    {{^dropdown}}
      <li class="nav-item">
        {{^leaf}}
          <span class="expand-stub"></span>
        {{/leaf}}
        {{#topicHref}}
          <a class="nav-link" href="{{topicHref}}" title="{{name}}">{{name}}</a>
        {{/topicHref}}
        {{^topicHref}}
          <a class="nav-link">{{{name}}}</a>
        {{/topicHref}}
        {{^leaf}}
          {{>partials/navbar-li}}
        {{/leaf}}
      </li>
    {{/dropdown}}
    {{#dropdown}}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{name}}</a>
        <ul class="dropdown-menu level{{level}}">
          {{>partials/dd-li}}
        </ul>
      </li>
    {{/dropdown}}
  {{/items}}
</ul>
