# Installation Guide

This file is in the `guides/` folder which **does not have its own toc.yml**.

Therefore, it will be automatically added to the root TOC as a nested item under "Guides".

## Installation Steps

1. Download docfx
2. Extract to a folder
3. Add to PATH
4. Run `docfx --version`
