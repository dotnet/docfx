# Unlisted File

This file exists in the `reference/` folder but is **not listed** in `reference/toc.yml`.

Since `auto: false` is set, this file will NOT appear in the TOC.

This demonstrates how `auto: false` gives you manual control.
