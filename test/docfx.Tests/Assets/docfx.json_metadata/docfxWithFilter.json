{
  "metadata": [
    {
      "src": [
        {
          "files": [ "**/*.csproj" ],
          "exclude": [ "**/bin/**", "**/obj/**" ], // `exclude` is also relative to `cwd`
          "cwd": "../src"
        }
      ],
      "dest": ".",
      "filter": "filter.yaml"
    }
  ]
}