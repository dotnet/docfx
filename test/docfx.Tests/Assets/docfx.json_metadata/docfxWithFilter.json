{
  "metadata": [
    {
      "src": [
        {
          "files": [ "**/*.csproj" ],
          "exclude": [ "**/bin/**", "**/obj/**" ], // `exclude` is also relative to `src`
          "src": "../src"
        }
      ],
      "dest": ".",
      "filter": "filter.yaml"
    }
  ]
}
