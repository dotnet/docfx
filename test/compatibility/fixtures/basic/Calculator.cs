namespace Compatibility.Basic;

/// <summary>A small public API used to verify metadata extraction.</summary>
public class Calculator
{
    /// <summary>Adds two numbers.</summary>
    public int Add(int left, int right) => left + right;
}
